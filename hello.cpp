#include<iostream>

int main()
{
    int a, b;  // Declare variables a and b to store the input integers

    std::cin >> a >> b;  // Read two integers from the standard input
    std::cout << a + b;  // Output the sum of the two integers
    
    return 0;  // Indicate successful completion of the program
}
